pipeline {
    agent { 
        dockerfile true
    }
	//agent any
    //agent { label 'master' }
    stages {
        stage('build') {
            steps {
                echo "Hello World!"
                //bat 'npm --version'
            }
        }
    }
}