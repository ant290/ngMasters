pipeline {
    // agent { docker { image 'node:6.3' } }
	agent any
    stages {
        stage('build') {
            steps {
                bat 'npm --version'
				bat 'ECHO hello world'
            }
        }
    }
}